import Link from "next/link"; import { ArrowIcon } from "./Icons"; import { LocaleSwitcher } from "./LocaleSwitcher"; import type { Dictionary } from "@/locales"; import type { Locale } from "@/locales/config";
export function SiteHeader({locale,d}:{locale:Locale;d:Dictionary}){const links=[[d.nav.products,"products"],[d.nav.factory,"factory"],[d.nav.videos,"videos"],[d.nav.downloads,"downloads"],[d.nav.contact,"contact"]];return <header className="site-header"><Link className="brand" href={`/${locale}`}><span className="brand-mark">IM</span><span><b>IRONMIX</b><small>HEAVY INDUSTRIES</small></span></Link><nav className="desktop-nav">{links.map(([label,url])=><Link key={url} href={`/${locale}/${url}`}>{label}</Link>)}</nav><div className="header-actions"><LocaleSwitcher locale={locale} label={d.nav.language}/><Link className="header-quote" href={`/${locale}/contact`}>{d.nav.quote} <ArrowIcon/></Link><details className="mobile-menu"><summary aria-label="Menu">☰</summary><nav>{links.map(([label,url])=><Link key={url} href={`/${locale}/${url}`}>{label}</Link>)}</nav></details></div></header>}
