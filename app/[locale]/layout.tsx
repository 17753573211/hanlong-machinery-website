import { notFound } from "next/navigation"; import type { Metadata } from "next"; import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { WhatsAppButton } from "@/components/WhatsAppButton"; import { JsonLd } from "@/components/JsonLd"; import { buildMetadata, organizationSchema, websiteSchema } from "@/lib/seo"; import { getDictionary } from "@/locales"; import { isLocale, locales } from "@/locales/config";
export function generateStaticParams(){return locales.map(locale=>({locale}))}
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return buildMetadata(locale,"",`${d.meta.title} | HANLONG GROUP`,d.meta.description)}
export default async function LocaleLayout({children,params}:{children:React.ReactNode;params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <div lang={locale==="zh"?"zh-CN":locale}><JsonLd data={[organizationSchema(),websiteSchema()]}/><SiteHeader locale={locale} d={d}/>{children}<SiteFooter locale={locale} d={d}/><WhatsAppButton label={d.common.whatsapp} className="whatsapp"/></div>}
