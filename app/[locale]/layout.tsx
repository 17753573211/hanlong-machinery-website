import { notFound } from "next/navigation"; import type { Metadata } from "next"; import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { getDictionary } from "@/locales"; import { isLocale, locales } from "@/locales/config";
export function generateStaticParams(){return locales.map(locale=>({locale}))}
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return{title:{default:`IRONMIX | ${d.meta.title}`,template:`%s | IRONMIX`},description:d.meta.description,alternates:{canonical:`/${locale}`,languages:{en:"/en",zh:"/zh",pt:"/pt",es:"/es",fr:"/fr","x-default":"/en"}}}}
export default async function LocaleLayout({children,params}:{children:React.ReactNode;params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <div lang={locale==="zh"?"zh-CN":locale}><SiteHeader locale={locale} d={d}/>{children}<SiteFooter locale={locale} d={d}/><a className="whatsapp" href="https://wa.me/" target="_blank" rel="noreferrer" aria-label={d.contact.whatsapp}><span>●</span><b>WhatsApp</b></a></div>}
