import Image from "next/image"; import { notFound } from "next/navigation"; import { PageHero } from "@/components/PageHero"; import { getDictionary } from "@/locales"; import { isLocale } from "@/locales/config"; import { factoryContent } from "@/src/data/site-content";
export default async function Factory({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const f=getDictionary(locale).factory;return <main><PageHero eyebrow={f.eyebrow} title={f.title} description={f.description}/><section className="factory-photo real-factory"><div><span>{f.overview}</span><strong>{f.overviewText}</strong></div></section><section className="section"><div className="container split-heading"><div><p className="eyebrow dark"><span/> {f.production}</p><h2>{f.processTitle}</h2></div><p className="lead">{f.processText}</p></div><div className="container factory-material-grid">{f.sections.map((x,i)=><article key={x.title}><div><Image src={factoryContent.sections[i].image} alt={x.title} fill sizes="50vw"/></div><span>0{i+1}</span><h3>{x.title}</h3><p>{x.text}</p></article>)}</div></section></main>}
