import { notFound } from "next/navigation"; import { PageHero } from "@/components/PageHero"; import { ArrowIcon } from "@/components/Icons"; import { getDictionary } from "@/locales"; import { isLocale } from "@/locales/config";
export default async function Downloads({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale).downloads,docs=[[d.spec08,d.spec08Type,"/media/documents/hl-08-specification.pdf"],[d.manual35,d.manual35Type,"/media/documents/hl-35-user-manual.pdf"]];return <main><PageHero eyebrow={d.eyebrow} title={d.title} description={d.description}/><section className="section"><div className="container download-list">{docs.map(([a,b,url],i)=><article key={a}><span>0{i+1}</span><div><h2>{a}</h2><p>{b}</p></div><a className="download-action" href={url} download><ArrowIcon/></a></article>)}</div></section></main>}
