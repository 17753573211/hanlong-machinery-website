import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowIcon } from "@/components/Icons"; import { getDictionary } from "@/locales"; import { isLocale } from "@/locales/config"; import { products } from "@/src/data/products";
export default async function Detail({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale),x=d.detail,p=products.find(y=>y.model==="HL-3.5")!;return <main><section className="detail-hero"><div className="container detail-grid"><div><p className="eyebrow"><span/> {x.eyebrow}</p><h1>HL-3.5</h1><p>{x.description}</p><div className="button-row"><Link className="button button-light" href={`/${locale}/contact`}>{x.requestPrice}<ArrowIcon/></Link><Link className="button button-ghost" href={`/${locale}/downloads`}>{x.downloadSpecs}</Link></div></div><div className="detail-machine"><div className="machine-silhouette"><span className="drum"/><span className="cab"/><span className="bucket"/><span className="wheel w1"/><span className="wheel w2"/></div></div></div></section><section className="spec-strip"><div><small>{x.output}</small><strong>3.5 m³</strong></div><div><small>{x.drive}</small><strong>4 × 4</strong></div><div><small>{x.control}</small><strong>{x.operator}</strong></div><div><small>{x.configuration}</small><strong>{x.options}</strong></div></section><section className="section"><div className="container split-heading"><div><p className="eyebrow dark"><span/> {x.capability}</p><h2>{x.title}</h2></div><p className="lead">{x.text}</p></div><div className="container detail-panels">{x.features.map((f,i)=><article key={f.title}><span>0{i+1}</span><h3>{f.title}</h3><p>{f.text}</p></article>)}</div></section><section className="section dark-section"><div className="container"><p className="eyebrow"><span/> {x.technical}</p><h2>{x.specifications}</h2><div className="spec-table">{p.specs.map(s=><div key={s.label}><span>{s.label}</span><strong>{s.value}</strong></div>)}</div></div></section></main>}
