import { notFound } from "next/navigation"; import { PageHero } from "@/components/PageHero"; import { getDictionary } from "@/locales"; import { isLocale } from "@/locales/config";
export default async function Videos({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const v=getDictionary(locale).videos;return <main><PageHero eyebrow={v.eyebrow} title={v.title} description={v.description}/><section className="section"><div className="container featured-video"><video controls preload="metadata"><source src="/media/videos/general-demo.mp4" type="video/mp4"/></video><small>{v.demo}</small><h2>{v.demoTitle}</h2></div><div className="container catalog-note">{v.note}</div></section></main>}
